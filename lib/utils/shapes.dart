import 'package:flutter/material.dart';

class RoundedShape {
  static BorderRadius small = BorderRadius.circular(
    20,
  );

  static BorderRadius medium = BorderRadius.circular(
    30,
  );

  static BorderRadius large = BorderRadius.circular(
    50,
  );
}
